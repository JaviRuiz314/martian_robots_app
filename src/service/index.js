'use strict';

const 
    Robot = require('./robotService'),
    ObjectTracker = require('./objectTrackerService'),
    Command = require('./commandService');

module.exports = {
    Robot,
    ObjectTracker,
    Command
}