'use strict';

const 
    Robot = require('./robotService'),
    ObjectTracker = require('./objectTracker'),
    Command = require('./commandService');

module.exports = {
    Robot,
    ObjectTracker,
    Command
}