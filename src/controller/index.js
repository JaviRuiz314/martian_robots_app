'use strict';

const 
    Robot = require('./robotController'),
    ObjectTracker = require('./objectTracker'),
    Command = require('./commadController');

module.exports = {
    Robot,
    ObjectTracker,
    Command
}